import Foundation
// We have programs balancing on discs with no discs of their own

// You ask each program to yell out their _name_, _weight_, and (if they're holding a disc) their _names of the programs immediately above them_ balancing on that disc

// The programs respond out of order.

